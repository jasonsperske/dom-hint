---
bump: minor
---

Add optional observation of `<head>` mutations and open shadow DOM roots. Both are opt-in via the new "Observe scope" fieldset in the popup. Shadow root observation recursively attaches observers to existing and dynamically created shadow roots. Test page expanded with shadow DOM and head-mutation examples.
