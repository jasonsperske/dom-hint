---
bump: minor
---

Improved activation UX with three new options: toggle mode (modifier+click to start, modifier+click again to stop), a visual on-page indicator badge showing when observation is active (on by default), and auto-start mode that begins observing immediately on injection without requiring hotkey activation.
